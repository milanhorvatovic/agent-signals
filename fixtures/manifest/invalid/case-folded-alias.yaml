- name: pr-comments
  command: ["./watchers/pr-comments.sh"]
  description: first
  trigger: session-start
  tiers: [mcp]
- name: PR-COMMENTS
  command: ["./watchers/pr-comments-2.sh"]
  description: an uppercase alias never reaches cross-entry alias detection
  trigger: session-start
  tiers: [mcp]
