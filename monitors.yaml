# Watcher manifest — the single source of truth (docs/event-contract.md
# §Manifest). Schema: schemas/monitors.schema.json. Adapter files under
# adapters/ will be generated from this file — never hand-edited.
#
# No sources are configured yet.
[]
